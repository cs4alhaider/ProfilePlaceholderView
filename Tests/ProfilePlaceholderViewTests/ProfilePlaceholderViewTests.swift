import XCTest
@testable import ProfilePlaceholderView

final class ProfilePlaceholderViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ProfilePlaceholderView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
